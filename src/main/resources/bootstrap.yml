spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/sedaasinek/config-server
          clone-on-start: true
          username: sedaasinek
          password:
server:
  port: 8980
  servlet:
    context-path: /onee-config-server